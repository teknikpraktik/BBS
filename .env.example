# Base URL of the sync backend. Leave unset to run BBS fully local: completed
# workouts stay in IndexedDB and are marked pending. Nothing else changes.
#
# Local reference backend:  npm run server
# VITE_SYNC_ENDPOINT=http://localhost:8787
VITE_SYNC_ENDPOINT=
